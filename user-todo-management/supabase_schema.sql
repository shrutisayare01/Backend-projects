create extension if not exists "uuid-ossp";
create table users(id uuid primary key default uuid_generate_v4(),name text,email text unique,password text,created_at timestamp default now());
create table todos(id uuid primary key default uuid_generate_v4(),title text,description text,is_completed boolean default false,user_id uuid references users(id) on delete cascade,created_at timestamp default now());